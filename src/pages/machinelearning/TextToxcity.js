import React, { useState, useEffect } from "react";

const TextToxcity = () => {
  const threshold = 0.9;

  //   const [model, SetModel] = useState(null);

  //   useEffect(() => {
  //     const loadModel = async () => {
  //       const model = await toxicity.load(threshold);
  //       console.log(model);
  //     };
  //   }, []);

  return <div>TextToxcity</div>;
};

export default TextToxcity;
